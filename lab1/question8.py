# write a python program which accepts the radius of circle from the user and compute area
radius = int(input('enter the radius'))
area = (22/7) * radius * radius
print(f"the area of circle is {area}")

